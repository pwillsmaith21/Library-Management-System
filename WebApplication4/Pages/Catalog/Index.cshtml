@page
@model WebApplication4.Pages.Catalog.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemstockT[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemstockT[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemstockT[0].ItemNumberNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ItemstockT) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemNumberNavigation.ItemId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ItemNumber">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ItemNumber">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ItemNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
